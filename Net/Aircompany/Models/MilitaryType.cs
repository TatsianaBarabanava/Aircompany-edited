namespace Aircompany.Models
{
    public enum MilitaryType
    {
        Fighter, //capitalized only first letter in Fighter
        Bomber, //capitalized only first letter in Bomber
        Transport //capitalized only first letter in Transport
    }
}
